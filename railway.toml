   [build]
   command = """
   apt-get update -qq && \
   apt-get upgrade -y && \
   apt-get install -y libsqlite3-0 libpq5 ca-certificates && \
   apt-get clean && \
   rm -rf /var/lib/apt/lists/*
   """